package seleniumTutorial0;
/*
 * Google interview question:
 * Google LinkedList Question:

Please use this Google doc to code during your interview. To free your hands for coding, we recommend that you use a headset or a phone with speaker option.

Rearrange a linked list containing a single char per node so that each word is reversed but the overall word order is preserved thus:
    (H)->(e)->(y)->(‘ ‘)->(w)->(o)->(r)->(l)->(d)->None
becomes:
    (y)->(e)->(H)->(‘ ‘)->(d)->(l)->(r)->(o)->(w)->None

 
package Selenium;


public class linkedlist {
	static node head;

	static class node {

int data;node,Next;

Node(int d){
data = d;
Next = Null;
}
	}

Node reversefunction(Node node){
Node i = head;
Node j = head;
node tepm = head;
String s = “”;

While(temp !=null){
if(temp.val==null || temp.next==null){
if (temp.val== “ “ ){
s=s.temp.val;

int z= s.length()-1;
While(z!=-1)(
i.val=s.charAt(z--);
i=i.next;

)
s=””;
i=temp.next;
j=temp.next;
else{
s+= temp.val;
j=j.next;

}


temp=temp.next;

}

}

}

}

	return head;
}}
*/